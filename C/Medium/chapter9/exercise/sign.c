// chpt. 9; c exercises

#include <stdio.h>

int main(void){
    // print 200 with-out sign 
    puts("Write 200 both with and without sign. Put some spacing!");
    printf(
        "  %d\n %+d\n %d\n",
        200,
        200,
        -200
    );
}